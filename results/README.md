# Local Results Directory

This directory is intentionally kept in the repository as an empty placeholder.

Expected local outputs include:

- benchmark JSON/CSV files
- experiment summaries
- generated plots
- calibration files

Raw experiment outputs are not versioned by default.
