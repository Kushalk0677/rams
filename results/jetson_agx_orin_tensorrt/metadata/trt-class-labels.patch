diff --git a/rams/models.py b/rams/models.py
index a62c80a..a5b1e28 100644
--- a/rams/models.py
+++ b/rams/models.py
@@ -26,6 +26,7 @@ from __future__ import annotations
 
 import time
 import math
+import re
 import random
 import logging
 import os
@@ -88,6 +89,32 @@ COCO_NAMES = [
 ]
 
 
+_TRT_PLACEHOLDER_RE = re.compile(r"class\d+")
+
+
+def _canonical_trt_class_name(names, cls_id: int, backend: str) -> str:
+    """Map TensorRT engine class ids onto the canonical COCO labels.
+
+    TensorRT engines built from the YOLOv8 ONNX exports carry placeholder
+    names (class0...classN) instead of COCO names, so ultralytics returns
+    class2/class5. Engine outputs are COCO-class-ordered; verified on this
+    Jetson against the ONNX path on the same frame (identical boxes with
+    canonical labels in results/full-20260908-v1 preflights). The canonical
+    label list is the single source of truth shared with the ONNX parser:
+    COCO_NAMES defined above in this module (same list the ONNX/PyTorch
+    paths use). Out-of-range ids degrade to str(cls_id) exactly like the
+    ONNX parser. Non-placeholder engine metadata names are kept verbatim,
+    and the remap fires only when backend == 'tensorrt' so ONNX/PyTorch
+    behaviour is byte-for-byte unchanged.
+    """
+    raw = str(names[cls_id] if isinstance(names, dict) else names[cls_id])
+    if backend == "tensorrt" and _TRT_PLACEHOLDER_RE.fullmatch(raw):
+        if 0 <= cls_id < len(COCO_NAMES):
+            return COCO_NAMES[cls_id]
+        return str(cls_id)
+    return raw
+
+
 def _parse_onnx_output(outputs, conf_thresh: float = 0.25) -> list[dict]:
     """Parse a YOLOv8 ONNX Runtime output array into detection dicts.
 
@@ -489,7 +516,8 @@ class ModelWrapper:
         inference_ms = (time.perf_counter() - inference_started) * 1000.0
         postprocess_started = time.perf_counter()
         dets = [
-            {"class": r.names[int(b.cls)], "conf": float(b.conf),
+            {"class": _canonical_trt_class_name(r.names, int(b.cls), self._backend),
+             "conf": float(b.conf),
              "xyxy":  b.xyxy.tolist()}
             for r in res for b in r.boxes
         ]
diff --git a/scripts/run_corrected_suite.py b/scripts/run_corrected_suite.py
index 01367a2..8ff26ec 100644
--- a/scripts/run_corrected_suite.py
+++ b/scripts/run_corrected_suite.py
@@ -69,6 +69,20 @@ def main():
                 for det in result['detections']:
                     assert len(det['xyxy']) == 4 and all(isinstance(x, float) for x in det['xyxy'])
                 evidence[tier.name] = result
+            if args.backend == 'tensorrt':
+                from rams.models import COCO_NAMES as _coco
+                _coco_set = set(_coco)
+                for tier in Tier:
+                    labels = [d['class'] for d in evidence[tier.name]['detections']]
+                    for label in labels:
+                        if label not in _coco_set:
+                            raise RuntimeError(
+                                f'TensorRT canonical-label gate failed for {tier.name}: {label!r} is not a canonical COCO label')
+                    evidence[tier.name]['trt_class_label_map'] = {
+                        'source': 'COCO_NAMES in rams/models.py — single source of truth shared with the ONNX parser',
+                        'engine_id_space': 'engine class id -> canonical label',
+                        'mapping': {str(i): n for i, n in enumerate(_coco)},
+                        'labels_seen': labels}
             (out/'backend_preflight.json').write_text(json.dumps(evidence, indent=2)+'\n')
         finally:
             library.unload_all()
